kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # API Gateway
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
      # InfluxDB
      - containerPort: 30086
        hostPort: 30086
        protocol: TCP
